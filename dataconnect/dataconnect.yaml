specVersion: "v1"
serviceId: "pucpflow"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pucpflow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
