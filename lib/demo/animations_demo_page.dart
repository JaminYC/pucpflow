import 'package:flutter/material.dart';

class AnimationsDemoPage extends StatelessWidget {
  const AnimationsDemoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Animations Demo')),
      body: const Center(child: Text('Demo Page')),
    );
  }
}
