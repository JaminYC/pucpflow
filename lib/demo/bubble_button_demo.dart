import 'package:flutter/material.dart';

class BubbleButtonDemo extends StatelessWidget {
  const BubbleButtonDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bubble Button Demo')),
      body: const Center(child: Text('Demo Page')),
    );
  }
}
